export type userType = {
  name: string;
  username: string;
  email: string;
  password: string;
};

export type userPayload = {
  id: string;
  name: string;
  email: string;
  username: string;
};
